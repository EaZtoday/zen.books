import LocationPage from './LocationPage';
import { lehighValleyData } from './locationData';

export default function LehighValleyBookkeeping() {
  return <LocationPage data={lehighValleyData} />;
}
